package compiler.commands;

import compiler.components.AssemblyArgument;

public class Move extends AssemblyCommand{
    public Move(AssemblyArgument from, AssemblyArgument to) {
        //TODO
    }
    public Move(AssemblyArgument from, AssemblyArgument to, int bytes) {
        //TODO
    }
}
