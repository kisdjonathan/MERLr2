package compiler.commands.arithmetic;

import compiler.components.AssemblyArgument;
import compiler.components.Register;

public class RoundFloat extends ArithmeticCommand {
    public RoundFloat(){}

    public RoundFloat(AssemblyArgument arg, Register register, int size) {
        //TODO
    }
}
