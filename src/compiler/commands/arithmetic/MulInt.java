package compiler.commands.arithmetic;

import compiler.components.AssemblyArgument;
import compiler.components.Register;

public class MulInt extends ArithmeticCommand {
    public MulInt(){}

    public MulInt(AssemblyArgument arg, Register register, int size) {
        //TODO
    }
}
